"use client";

import React from "react";

export default function AriseUnifiedMapPage() {
  // Returns an empty container so the underlying Unity canvas renders completely unobstructed
  return <div className="w-full h-full pointer-events-none" />;
}